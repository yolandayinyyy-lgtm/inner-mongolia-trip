# 内蒙古 7天6晚旅行

2026年10月2日至8日，4位同行。React + TypeScript + Vite + Tailwind CSS + Lucide。手机优先，桌面端居中，最大宽度430px。

## 本地运行

需要 Node.js 22+ 与 pnpm。

```sh
pnpm install
pnpm dev
```

构建与检查：

```sh
pnpm build
pnpm preview
```

构建输出 `dist/`。`pnpm build` 先执行 TypeScript 检查。

## 页面与数据

- `/#/overview` 总览；`/#/start` 启程；`/#/day1` 至 `/#/day5`；`/#/return` 返程。
- `/#/bookings` 总预订；`/#/transport` 交通总览；`/#/checklist` 行李清单。
- 所有实际行程、住宿、餐饮、项目、时间轴、预订数据位于 `src/data/itinerary.ts`。酒店未选定，价格、URL和具体车次没有虚构。
- 可更新 BookingItem 的 `url`、`price`、`status` 和 `note`，卡片 `url` 决定详情按钮是否开放。每日卡片与预订清单各自有链接字段，需要同步维护。
- 预订有四种状态：待预订、已预留、已付款、已确认。状态仅保存在当前浏览器，更新不产生真实订单。
- 本地存储键：`mongolia:bookings:v1`、`mongolia:packing:v1`。本地记录会覆盖数据文件内的初始状态，修改初始值后测试请清除对应键。
- 顶部日期可横滑；每日内容区支持左右滑动切天；底部前后导航；右上打印全套。
- 首页 SVG 地图为方位示意，长沙单列，不是导航。每日 SVG 为行程顺序图。
- 非必需的 WebMCP API 通过能力检测注册，只读取行程或修改本地预订记录。未支持该 API 的浏览器正常使用。

## 内容口径

采用2026年9月15日新附件：19:00暂定抵达（旧讨论为16:50），具体航班待确认。10/5的10:30—11:00解释为出发窗口，不是城际行车时长。全部时间为计划，不是实测车程或已订票。

妆造安排在旅拍前。10/4拍摄地待商家确定，并未默认可以从火山即时到达呼市旅拍。滑草长度、小火车长度、节目、泳池、国庆开放、博物馆预约、机场与高铁站待核实。

## 待补充资料 / 可替换资产

1. 往返航班号、机场、时刻、行李额度。
2. 各晚酒店名称、地址、房型、价格和预订链接。
3. 马场、滑草、小火车运营商、长度与开放状态。
4. 旅拍地点、商家、价格、精修与服装归还安排。
5. 天马盛会场次、票价、官方购票链接。
6. 自然博物馆国庆预约安排与官方入口。
7. 高铁具体车次 / 车站，接送与租车价格及链接。
8. 响沙湾套票内容、项目位置、住宿接驳与水上项目开放。
9. `public/images/autumn.webp`：原创AI旅行意境插画（非实景、非地图），可替换为有使用权的照片。原始生成图另存于Downloads，不随项目发布。
10. `src/components/TerrainMap.tsx` 与 App.tsx 的 SmallMap：可编辑的SVG路线结构；标签与路线不固化在位图。

## Vercel 部署步骤

1. 将完整项目推送到自己的 GitHub 仓库。
2. 在 Vercel 新建项目，导入仓库，选择 Vite。
3. Install Command：`pnpm install --frozen-lockfile`；Build Command：`pnpm build`；Output Directory：`dist`。
4. 点击 Deploy。此项目无后端和环境变量要求，使用 hash 路由，无需额外 rewrite。
5. 在手机打开发布链接，核对切天、勾选及刷新保存。

也可在 Netlify 使用同样的构建命令和输出目录。Sites 发布资料由 `.openai/hosting.json` 管理；如在其他平台部署，此文件不影响前端。

## 验证范围

检查构建、11个页面、320/375/390/430px宽度、预订与勾选持久化、前后导航和无效路由。真实 iPhone Safari、安卓微信内置浏览器仍需设备验收；浏览器自动化不等价于真机保证。
